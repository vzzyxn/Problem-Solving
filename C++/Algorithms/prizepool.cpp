#include <iostream>
using namespace std;

int main() {
		int t;
		cin >> t;
		for(int i = 1; i <=t; i++) {
			int x, y, top_ten, top_ninety, total;
			cin >> x >> y;q
			top_ten = (x * 10 );
			top_ninety = (y * 90);
			total = top_ten + top_ninety;
			cout << total << "\n";
		}
			
	return 0;
}
